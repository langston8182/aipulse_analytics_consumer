{
  "name": "ai_pulse_analytics_consumer",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "bestzip build.zip * --ignore=node_modules/aws-sdk/* --ignore=.git",
    "deploy": "npm run build && echo 'Deploying zip file...'"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "bestzip": "^2.2.1"
  },
  "dependencies": {
    "mongoose": "^8.10.1"
  }
}
